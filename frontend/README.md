# Frontend - UI of the portfolio

### To start the app on local

Run `npm start` from terminal

### To deploy to gh-pages

Two simple steps to publish the changes to gh-pages

1. Run `npm run build` from terminal
2. Run `npm run deploy` from terminal


